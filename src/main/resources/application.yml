server:
  port: 8200
spring:
  application:
    name: admin-service
  config:
    activate:
      on-profile: local, dev, local
    import: optional:configserver:http://localhost:8888
management:
  endpoints:
    web:
      exposure:
        include:
          - "refresh"
          - "bus-refresh"